import 'package:caculator_flutter/main.dart';

class Math {
  static String consolve(String text,btnText) {
    double num1;
    double num2;
    dynamic result = ''; //để lưu giá trị tạm thời nhập
    dynamic finalResult = '';
    dynamic preOpr = ''; //đọc toán hạng
    dynamic opr = '';
    result = result + btnText;
    text = result as String;
    return text;
  }
}